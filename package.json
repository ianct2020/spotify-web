{
  "name": "spotify-web",
  "version": "1.0.0",
  "description": "Personal Spotify library management tool",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 5500 --directory src",
    "build": "rm -rf docs/* && cp -r src/* docs/ && touch docs/.nojekyll && echo 'Build OK → docs/'",
    "deploy": "npm run build && cd ~/spotify-web && git add docs/ && git commit -m 'chore: rebuild docs' && git push"
  }
}
